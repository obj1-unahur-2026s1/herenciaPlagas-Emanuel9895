import plagas.*


describe "probar tacar con las plagas a los elementos" {

  method initialize(){
    const plagaMosquito1 = new Mosquitos(poblacion = 15)
    const hogar1 =  new ()
  }

  test "pepita has initial energy" {
    assert.equals(100, pepita.energy())
  }

}